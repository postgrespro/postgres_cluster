<!-- doc/src/xml/auth-delay.xml -->

<sect1 id="auth-delay" xreflabel="auth_delay">
 <title>auth_delay</title>

 <indexterm zone="auth-delay"><primary>auth_delay</primary></indexterm>

 <para>Модуль <filename>auth_delay</filename> добавляет небольшую задержку в процессе проверки подлинности перед тем, как выдаётся сообщение об ошибке, чтобы усложнить подбор паролей к базам данных. Заметьте, что это никоим образом не препятствует атакам типа &laquo;отказ в обслуживании&raquo;, а даже наоборот, может помочь их осуществить, так как процессы, ожидающие сообщения об ошибке, всё равно занимают слоты подключения.</para>

 <para>Чтобы эта функция работала, данный модуль нужно загрузить посредством параметра конфигурации <xref linkend="guc-shared-preload-libraries"/> в <filename>postgresql.conf</filename>.</para>

 <sect2>
  <title>Параметры конфигурации</title>

  <variablelist>
   <varlistentry>
    <term><varname>auth_delay.milliseconds</varname> (<type>int</type>) <indexterm><primary>параметр конфигурации <varname>auth_delay.milliseconds</varname></primary></indexterm></term>
    <listitem>
     <para>Число миллисекунд, которое нужно подождать, прежде чем сообщать об ошибке аутентификации. По умолчанию 0.</para>
    </listitem>
   </varlistentry>
  </variablelist>

  <para>Эти параметры должны задаваться в <filename>postgresql.conf</filename>. Обычное использование выглядит так:</para>

<programlisting># postgresql.conf
shared_preload_libraries = 'auth_delay'

auth_delay.milliseconds = '500'</programlisting>
 </sect2>

 <sect2>
  <title>Автор</title>

  <para>КайГай Кохэй <email>kaigai@ak.jp.nec.com</email></para>
 </sect2>

</sect1>
