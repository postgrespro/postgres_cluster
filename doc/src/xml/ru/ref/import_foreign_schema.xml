<!--
doc/src/xml/ref/import_foreign_schema.xml
&productname; documentation
-->

<refentry id="sql-importforeignschema">
 <indexterm zone="sql-importforeignschema"><primary>IMPORT FOREIGN SCHEMA</primary></indexterm>

 <refmeta>
  <refentrytitle>IMPORT FOREIGN SCHEMA</refentrytitle>
  <manvolnum>7</manvolnum>
  <refmiscinfo>Операторы языка SQL</refmiscinfo>
 </refmeta>

 <refnamediv>
  <refname>IMPORT FOREIGN SCHEMA</refname>
  <refpurpose>импортировать определения таблиц со стороннего сервера</refpurpose>
 </refnamediv>

 <refsynopsisdiv>
<synopsis>IMPORT FOREIGN SCHEMA <replaceable class="parameter">удалённая_схема</replaceable>
    [ { LIMIT TO | EXCEPT } ( <replaceable class="parameter">имя_таблицы</replaceable> [, ...] ) ]
    FROM SERVER <replaceable class="parameter">имя_сервера</replaceable>
    INTO <replaceable class="parameter">локальная_схема</replaceable>
    [ OPTIONS ( <replaceable class="parameter">параметр</replaceable> '<replaceable class="parameter">значение</replaceable>' [, ... ] ) ]</synopsis>
 </refsynopsisdiv>

 <refsect1 id="sql-importforeignschema-description">
  <title>Описание</title>

  <para><command>IMPORT FOREIGN SCHEMA</command> создаёт сторонние таблицы, которые представляют таблицы, существующие на стороннем сервере. Новые сторонние таблицы будут принадлежать пользователю, выполняющему команду, и будут содержать корректные определения столбцов и параметры, соответствующие удалённым таблицам.</para>

  <para>По умолчанию импортируются все таблицы и представления, существующие в определённой схеме на стороннем сервере. По желанию список таблиц можно ограничить некоторым подмножеством, или исключить из него конкретные таблицы. Новые сторонние таблицы создаются в целевой схеме, которая должна уже существовать.</para>

  <para>Чтобы использовать <command>IMPORT FOREIGN SCHEMA</command>, необходимо иметь право <literal>USAGE</literal> для стороннего сервера, а также право <literal>CREATE</literal> в целевой схеме.</para>
 </refsect1>

 <refsect1>
  <title>Параметры</title>

  <variablelist>

   <varlistentry>
    <term><replaceable class="parameter">удалённая_схема</replaceable></term>
    <listitem>
     <para>Удалённая схема, из которой будут импортированы объекты. Что именно представляет собой удалённая схема, зависит от применяемой обёртки сторонних данных.</para>
    </listitem>
   </varlistentry>

   <varlistentry>
    <term><literal>LIMIT TO ( <replaceable class="parameter">имя_таблицы</replaceable> [, ...] )</literal></term>
    <listitem>
     <para>Импортировать только сторонние таблицы с заданными именами. Другие таблицы, существующие в сторонней схеме, будут проигнорированы.</para>
    </listitem>
   </varlistentry>

   <varlistentry>
    <term><literal>EXCEPT ( <replaceable class="parameter">имя_таблицы</replaceable> [, ...] )</literal></term>
    <listitem>
     <para>Исключить из импорта указанные сторонние таблицы. Данная команда импортирует все таблицы, существующие в сторонней схеме, за исключением перечисленных в этом предложении.</para>
    </listitem>
   </varlistentry>

   <varlistentry>
    <term><replaceable class="parameter">имя_сервера</replaceable></term>
    <listitem>
     <para>Сторонний сервер, с которого импортируется схема.</para>
    </listitem>
   </varlistentry>

   <varlistentry>
    <term><replaceable class="parameter">локальная_схема</replaceable></term>
    <listitem>
     <para>Схема, в которой будут созданы импортируемые сторонние таблицы.</para>
    </listitem>
   </varlistentry>

   <varlistentry>
    <term><literal>OPTIONS ( <replaceable class="parameter">параметр</replaceable> '<replaceable class="parameter">значение</replaceable>' [, ...] )</literal></term>
    <listitem>
     <para>Параметры, которые должны применяться при импорте. Допустимые имена параметров и их значения зависят от обёртки сторонних данных.</para>
    </listitem>
   </varlistentry>
  </variablelist>
 </refsect1>

 <refsect1 id="sql-importforeignschema-examples">
  <title>Примеры</title>

  <para>Импорт определений таблиц из удалённой схемы <structname>foreign_films</structname> на сервере <structname>film_server</structname> с созданием сторонних таблиц в локальной схеме <structname>films</structname>: <programlisting>IMPORT FOREIGN SCHEMA foreign_films
    FROM SERVER film_server INTO films;</programlisting></para>

  <para>Та же операция, но импортируются только таблицы <structname>actors</structname> и <literal>directors</literal> (если они существуют): <programlisting>IMPORT FOREIGN SCHEMA foreign_films LIMIT TO (actors, directors)
    FROM SERVER film_server INTO films;</programlisting></para>

 </refsect1>

 <refsect1 id="sql-importforeignschema-compatibility">
  <title>Совместимость</title>

  <para>Команда <command>IMPORT FOREIGN SCHEMA</command> соответствует стандарту <acronym>SQL</acronym>, за исключением параметра <literal>OPTIONS</literal>, являющегося расширением <productname>&productname;</productname>.</para>

 </refsect1>

 <refsect1>
  <title>См. также</title>

  <simplelist type="inline">
   <member><xref linkend="sql-createforeigntable"/></member>
   <member><xref linkend="sql-createserver"/></member>
  </simplelist>
 </refsect1>
</refentry>
