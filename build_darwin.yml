platform: darwin

# image_resource:
#   type: docker-image
#   source: {repository: concourse/bin-ci}

inputs:
- name: postgresql
  path: postgresql

run:
  # path: ls
  # args: ["-la","postgresql"]
  path: postgresql/check.sh
